<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "mybatis-3-mapper.dtd">
  
  
<mapper namespace="tblproduct">
 	<!-- 리턴 타입이 List 일때에는 제너릭 타입만 설정하면 됩니다. -->
 	<select id="selectAll" resultType="">
 		select * from tbl_product order by product_date desc
 	</select>
 	<!-- insert, update, delete는 리턴타입 지정 안합니다.(그래도 값은 리턴해 줍니다.) -->
 	<insert id="insert" parameterType="ProductVo">
 	INSERT INTO TBL_PRODUCT  
                VALUES (#{category},#{pcode},#{pname},#{price})
 	</insert>
 	<update id="update" parameterMap="java.util.Map">
 	UPDATE tbl_buy 
                SET QUANTITY = #{quantity} 
                WHERE BUY_IDX = #{buy_idx}
 	</update>
 	<delete id="delete" parameterType="int">
 	DELETE FROM tbl_buy 
                WHERE BUY_IDX=#{buy_idx}
 	</delete>
 </mapper>